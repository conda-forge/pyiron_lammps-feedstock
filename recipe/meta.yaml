{% set name = "pyiron_lammps" %}
{% set version = "0.2.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyiron_lammps-{{ version }}.tar.gz
  sha256: e797d3f62a24a0b4ac6c764d192234ea2d2d29741ff08b5e84d215b73e9ff4ec

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
    - ase
    - atomistics
    - numpy
    - pandas
    - pylammpsmpi
    - pympipool
    - versioneer
    - toml
  run:
    - jinja2 >=2.11.3,<=3.1.2
    - python >=3.8
    - ase ==3.22.1
    - atomistics >=0.1.2,<=0.1.9
    - numpy >=1.26.0,<=1.26.2
    - pandas >=1.5.3,<=2.1.3
    - pylammpsmpi >=0.2.5,<=0.2.9
    - pympipool >=0.7.2,<=0.7.9

test:
  imports:
    - pyiron_lammps
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/pyiron/pyiron_lammps
  summary: pyiron_lammps - calculate material properties for interatomic potentials
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - srmnitc
    - pyiron-runner
    - jan-janssen
    - pmrv
    - niklassiemer
